import {
  "moonbitlang/async",
  "justjavac/proton",
  "moonbit_labeler/extensions/labeler" @labeler,
}

supported_targets = "native"

pkgtype(kind: "executable")

options(
  targets: { "main.mbt": [ "native" ] },
)
