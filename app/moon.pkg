import {
  "moonbitlang/async",
  "justjavac/proton",
  "moonbit_labeler/extensions/labeler",
}

supported_targets = "native"

pkgtype(kind: "executable")

options(
  "native-stub": [ "native_stub.c" ],
  targets: { "main.mbt": [ "native" ] },
)
